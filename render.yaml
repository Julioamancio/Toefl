databases:
  - name: toefl-db
    plan: free

services:
  - type: web
    name: toefl-dashboard
    env: python
    buildCommand: pip install -r requirements.txt
    startCommand: gunicorn app:app
    preDeployCommand: python simple_migration.py
    envVars:
      - key: FLASK_ENV
        value: production
      - key: SECRET_KEY
        generateValue: true
      - key: DATABASE_URL
        fromDatabase:
          name: toefl-db
          property: connectionString
      - key: AUTO_CREATE_TABLES
        value: "1"